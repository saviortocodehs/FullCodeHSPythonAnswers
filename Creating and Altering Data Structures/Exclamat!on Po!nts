def exclamation(text):
    # Convert to list
    text_list = list(text)
    
    # Replace instances of lowercase i
    for i in range(len(text_list)):
        if text_list[i] == "i":
            text_list[i] = "!"
    
    # Return result
    return "".join(text_list)
